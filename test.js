const { reverse } = require("dns");

let arr = [
    ['hi','check'],
    ['ha','check'],
    ['ho','fk'],
]


let resvert = arr.join(',')
let onlystr = resvert.join('\n')
resvert;
console.log(resvert)
let firstRow = arr[0];
let change1stRow = arr[0].splice(1,1,'uncheck')


firstRow;
arr;
